# Backend server port (Vite dev server uses port 3000)
PORT=8080

# Database credentials for Docker Compose
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# Optional: Set to 'production' to use built frontend assets
# RACK_ENV=production

# Optional: MapLibre style URL
# MAP_ID=REPLACE-ME
